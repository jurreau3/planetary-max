
diff --git a/src/types.ts b/src/types.ts
index 0000000..1111111 100644
--- a/src/types.ts
+++ b/src/types.ts
@@ -1,3 +1,7 @@
+// -------------------------------------------------------------
+// Core Bindings
+// -------------------------------------------------------------
 export type Bindings = {
   PLANETARY_MODE?: string;
   UMBRELLA_ENFORCEMENT?: string;
@@ -132,6 +136,13 @@ export type PlanetaryCanon = {
   truths: InstituteTruth[];
 };
 
+export type InstituteCanon = {
+  id: string;
+  truths: InstituteTruth[];
+};
+
+// -------------------------------------------------------------
+// Planetary Types
 export type PlanetaryState = {
   identity: PlanetaryIdentity;
   substrate: PlanetarySubstrate;
@@ -170,4 +181,43 @@ export type IntrospectionKind =
   | "planetary.identity"
   | "planetary.substrate"
   | "planetary.quantum"
-  | "planetary.canon"
-  | "planetary.governance";
+  | "planetary.canon"
+  | "planetary.governance";
+
+// -------------------------------------------------------------
+// Portal Kernel State
+// -------------------------------------------------------------
+export type PortalKernelState = {
+  overlay: QuantumOverlay;
+  collapsed: Record<string, unknown>;
+  facts: Record<string, unknown>[];
+  governance: GovernanceMetadata[];
+};
+
+// -------------------------------------------------------------
+// Governance Context
+// -------------------------------------------------------------
+export type GovernanceContext = {
+  metadata?: GovernanceMetadata[];
+};
+
+// -------------------------------------------------------------
+// Epistemic Types
+// -------------------------------------------------------------
+export type EpistemicEventAction = string;
+
+export type EpistemicEvent = {
+  id: string;
+  action: EpistemicEventAction;
+  payload: Record<string, unknown>;
+  createdAt: string;
+};
+
+export type EpistemicTimeline = {
+  events: EpistemicEvent[];
+};
